# siniciali.com

Personal portfolio website of Bruno Raphaël Siniciali.
Single-file static site (HTML/CSS/JS) deployed via GitHub Pages.

## Deploy

Copy `index.html` and `CNAME` into the repo, then:

```
git add .
git commit -m "New portfolio site"
git push
```
